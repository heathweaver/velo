/**
 * Route label for the cross-account inbox.
 *
 * Kept as a constant rather than a bare string so the list, the sidebar and the
 * router agree on one spelling — this label is deliberately not an account
 * label, and querying it omits the account filter entirely.
 */
export const ALL_INBOXES_LABEL = "all-inboxes";
